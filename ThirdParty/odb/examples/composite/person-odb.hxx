// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#ifndef PERSON_ODB_HXX
#define PERSON_ODB_HXX

#include <odb/version.hxx>

#if (ODB_VERSION != 20400UL)
#error ODB runtime version mismatch
#endif

#include <odb/pre.hxx>

#include "person.hxx"

#include <memory>
#include <cstddef>

#include <odb/core.hxx>
#include <odb/traits.hxx>
#include <odb/callback.hxx>
#include <odb/wrapper-traits.hxx>
#include <odb/pointer-traits.hxx>
#include <odb/container-traits.hxx>
#include <odb/no-op-cache-traits.hxx>
#include <odb/result.hxx>
#include <odb/simple-object-result.hxx>

#include <odb/details/unused.hxx>
#include <odb/details/shared-ptr.hxx>

namespace odb
{
  // person
  //
  template <>
  struct class_traits< ::person >
  {
    static const class_kind kind = class_object;
  };

  template <>
  class access::object_traits< ::person >
  {
    public:
    typedef ::person object_type;
    typedef ::person* pointer_type;
    typedef odb::pointer_traits<pointer_type> pointer_traits;

    static const bool polymorphic = false;

    typedef ::email_address id_type;

    static const bool auto_id = false;

    static const bool abstract = false;

    static id_type
    id (const object_type&);

    typedef
    no_op_pointer_cache_traits<pointer_type>
    pointer_cache_traits;

    typedef
    no_op_reference_cache_traits<object_type>
    reference_cache_traits;

    static void
    callback (database&, object_type&, callback_event);

    static void
    callback (database&, const object_type&, callback_event);
  };
}

#include <odb/details/buffer.hxx>

#include <odb/sqlite/version.hxx>
#include <odb/sqlite/forward.hxx>
#include <odb/sqlite/binding.hxx>
#include <odb/sqlite/sqlite-types.hxx>
#include <odb/sqlite/query.hxx>

namespace odb
{
  // basic_name
  //
  template <>
  class access::composite_value_traits< ::basic_name, id_sqlite >
  {
    public:
    typedef ::basic_name value_type;

    struct image_type
    {
      // first_
      //
      details::buffer first_value;
      std::size_t first_size;
      bool first_null;

      // last_
      //
      details::buffer last_value;
      std::size_t last_size;
      bool last_null;
    };

    static bool
    grow (image_type&,
          bool*);

    static void
    bind (sqlite::bind*,
          image_type&,
          sqlite::statement_kind);

    static bool
    init (image_type&,
          const value_type&,
          sqlite::statement_kind);

    static void
    init (value_type&,
          const image_type&,
          database*);

    static bool
    get_null (const image_type&);

    static void
    set_null (image_type&,
              sqlite::statement_kind);

    static const std::size_t column_count = 2UL;
  };

  // name_extras
  //
  template <>
  class access::composite_value_traits< ::name_extras, id_sqlite >
  {
    public:
    typedef ::name_extras value_type;

    struct image_type
    {
      // nickname_
      //
      details::buffer nickname_value;
      std::size_t nickname_size;
      bool nickname_null;
    };

    // aliases_
    //
    struct aliases_traits
    {
      typedef ::basic_names container_type;
      typedef
      odb::access::container_traits<container_type>
      container_traits_type;
      typedef container_traits_type::index_type index_type;
      typedef container_traits_type::value_type value_type;

      typedef ordered_functions<index_type, value_type> functions_type;
      typedef sqlite::container_statements< aliases_traits > statements_type;

      struct data_image_type
      {
        // index
        //
        long long index_value;
        bool index_null;

        // value
        //
        composite_value_traits< value_type, id_sqlite >::image_type value_value;

        std::size_t version;
      };

      static void
      bind (sqlite::bind*,
            const sqlite::bind* id,
            std::size_t id_size,
            data_image_type&);

      static void
      grow (data_image_type&,
            bool*);

      static void
      init (data_image_type&,
            index_type*,
            const value_type&);

      static void
      init (index_type&,
            value_type&,
            const data_image_type&,
            database*);

      static void
      insert (index_type, const value_type&, void*);

      static bool
      select (index_type&, value_type&, void*);

      static void
      delete_ (void*);

      static void
      persist (const container_type&,
               statements_type&);

      static void
      load (container_type&,
            statements_type&);

      static void
      update (const container_type&,
              statements_type&);

      static void
      erase (statements_type&);
    };

    static bool
    grow (image_type&,
          bool*);

    static void
    bind (sqlite::bind*,
          image_type&,
          sqlite::statement_kind);

    static bool
    init (image_type&,
          const value_type&,
          sqlite::statement_kind);

    static void
    init (value_type&,
          const image_type&,
          database*);

    static const std::size_t column_count = 1UL;
  };

  // name
  //
  template <>
  class access::composite_value_traits< ::name, id_sqlite >
  {
    public:
    typedef ::name value_type;

    struct image_type: composite_value_traits< ::basic_name, id_sqlite >::image_type
    {
      // title_
      //
      details::buffer title_value;
      std::size_t title_size;
      bool title_null;

      // extras_
      //
      composite_value_traits< ::name_extras, id_sqlite >::image_type extras_value;
    };

    static bool
    grow (image_type&,
          bool*);

    static void
    bind (sqlite::bind*,
          image_type&,
          sqlite::statement_kind);

    static bool
    init (image_type&,
          const value_type&,
          sqlite::statement_kind);

    static void
    init (value_type&,
          const image_type&,
          database*);

    static const std::size_t column_count = 4UL;
  };

  // phone_numbers
  //
  template <>
  class access::composite_value_traits< ::phone_numbers, id_sqlite >
  {
    public:
    typedef ::phone_numbers value_type;

    struct image_type
    {
      // first
      //
      details::buffer first_value;
      std::size_t first_size;
      bool first_null;

      // second
      //
      details::buffer second_value;
      std::size_t second_size;
      bool second_null;
    };

    static bool
    grow (image_type&,
          bool*);

    static void
    bind (sqlite::bind*,
          image_type&,
          sqlite::statement_kind);

    static bool
    init (image_type&,
          const value_type&,
          sqlite::statement_kind);

    static void
    init (value_type&,
          const image_type&,
          database*);

    static bool
    get_null (const image_type&);

    static void
    set_null (image_type&,
              sqlite::statement_kind);

    static const std::size_t column_count = 2UL;
  };

  // email_address
  //
  template <>
  class access::composite_value_traits< ::email_address, id_sqlite >
  {
    public:
    typedef ::email_address value_type;

    struct image_type
    {
      // recipient_
      //
      details::buffer recipient_value;
      std::size_t recipient_size;
      bool recipient_null;

      // domain_
      //
      details::buffer domain_value;
      std::size_t domain_size;
      bool domain_null;
    };

    static bool
    grow (image_type&,
          bool*);

    static void
    bind (sqlite::bind*,
          image_type&,
          sqlite::statement_kind);

    static bool
    init (image_type&,
          const value_type&,
          sqlite::statement_kind);

    static void
    init (value_type&,
          const image_type&,
          database*);

    static bool
    get_null (const image_type&);

    static void
    set_null (image_type&,
              sqlite::statement_kind);

    static const std::size_t column_count = 2UL;
  };

  // person
  //
  template <typename A>
  struct query_columns< ::person, id_sqlite, A >
  {
    // email
    //
    struct email_class_
    {
      email_class_ ()
      {
      }

      // recipient
      //
      typedef
      sqlite::query_column<
        sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::query_type,
        sqlite::id_text >
      recipient_type_;

      static const recipient_type_ recipient;

      // domain
      //
      typedef
      sqlite::query_column<
        sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::query_type,
        sqlite::id_text >
      domain_type_;

      static const domain_type_ domain;
    };

    static const email_class_ email;

    // name
    //
    struct name_class_
    {
      name_class_ ()
      {
      }

      // first
      //
      typedef
      sqlite::query_column<
        sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::query_type,
        sqlite::id_text >
      first_type_;

      static const first_type_ first;

      // last
      //
      typedef
      sqlite::query_column<
        sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::query_type,
        sqlite::id_text >
      last_type_;

      static const last_type_ last;

      // title
      //
      typedef
      sqlite::query_column<
        sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::query_type,
        sqlite::id_text >
      title_type_;

      static const title_type_ title;

      // extras
      //
      struct extras_class_1_
      {
        extras_class_1_ ()
        {
        }

        // nickname
        //
        typedef
        sqlite::query_column<
          sqlite::value_traits<
            ::std::string,
            sqlite::id_text >::query_type,
          sqlite::id_text >
        nickname_type_;

        static const nickname_type_ nickname;
      };

      static const extras_class_1_ extras;
    };

    static const name_class_ name;

    // phone
    //
    struct phone_class_
    {
      phone_class_ ()
      {
      }

      // first
      //
      typedef
      sqlite::query_column<
        sqlite::value_traits<
          ::std::basic_string< char >,
          sqlite::id_text >::query_type,
        sqlite::id_text >
      first_type_;

      static const first_type_ first;

      // second
      //
      typedef
      sqlite::query_column<
        sqlite::value_traits<
          ::std::basic_string< char >,
          sqlite::id_text >::query_type,
        sqlite::id_text >
      second_type_;

      static const second_type_ second;
    };

    static const phone_class_ phone;
  };

  template <typename A>
  const typename query_columns< ::person, id_sqlite, A >::email_class_::recipient_type_
  query_columns< ::person, id_sqlite, A >::email_class_::
  recipient (A::table_name, "\"email_recipient\"", 0);

  template <typename A>
  const typename query_columns< ::person, id_sqlite, A >::email_class_::domain_type_
  query_columns< ::person, id_sqlite, A >::email_class_::
  domain (A::table_name, "\"email_domain\"", 0);

  template <typename A>
  const typename query_columns< ::person, id_sqlite, A >::email_class_
  query_columns< ::person, id_sqlite, A >::email;

  template <typename A>
  const typename query_columns< ::person, id_sqlite, A >::name_class_::first_type_
  query_columns< ::person, id_sqlite, A >::name_class_::
  first (A::table_name, "\"name_first\"", 0);

  template <typename A>
  const typename query_columns< ::person, id_sqlite, A >::name_class_::last_type_
  query_columns< ::person, id_sqlite, A >::name_class_::
  last (A::table_name, "\"name_last\"", 0);

  template <typename A>
  const typename query_columns< ::person, id_sqlite, A >::name_class_::title_type_
  query_columns< ::person, id_sqlite, A >::name_class_::
  title (A::table_name, "\"name_title\"", 0);

  template <typename A>
  const typename query_columns< ::person, id_sqlite, A >::name_class_::extras_class_1_::nickname_type_
  query_columns< ::person, id_sqlite, A >::name_class_::extras_class_1_::
  nickname (A::table_name, "\"name_extras_nickname\"", 0);

  template <typename A>
  const typename query_columns< ::person, id_sqlite, A >::name_class_::extras_class_1_
  query_columns< ::person, id_sqlite, A >::name_class_::extras;

  template <typename A>
  const typename query_columns< ::person, id_sqlite, A >::name_class_
  query_columns< ::person, id_sqlite, A >::name;

  template <typename A>
  const typename query_columns< ::person, id_sqlite, A >::phone_class_::first_type_
  query_columns< ::person, id_sqlite, A >::phone_class_::
  first (A::table_name, "\"phone_first\"", 0);

  template <typename A>
  const typename query_columns< ::person, id_sqlite, A >::phone_class_::second_type_
  query_columns< ::person, id_sqlite, A >::phone_class_::
  second (A::table_name, "\"phone_second\"", 0);

  template <typename A>
  const typename query_columns< ::person, id_sqlite, A >::phone_class_
  query_columns< ::person, id_sqlite, A >::phone;

  template <typename A>
  struct pointer_query_columns< ::person, id_sqlite, A >:
    query_columns< ::person, id_sqlite, A >
  {
  };

  template <>
  class access::object_traits_impl< ::person, id_sqlite >:
    public access::object_traits< ::person >
  {
    public:
    struct id_image_type
    {
      composite_value_traits< ::email_address, id_sqlite >::image_type id_value;

      std::size_t version;
    };

    struct image_type
    {
      // email_
      //
      composite_value_traits< ::email_address, id_sqlite >::image_type email_value;

      // name_
      //
      composite_value_traits< ::person::name_type, id_sqlite >::image_type name_value;

      // phone_
      //
      composite_value_traits< ::phone_numbers, id_sqlite >::image_type phone_value;

      std::size_t version;
    };

    struct extra_statement_cache_type;

    // aliases_
    //
    struct name_extras_aliases_traits: access::composite_value_traits< ::name_extras, id_sqlite >::aliases_traits
    {
      static const std::size_t id_column_count = 2UL;
      static const std::size_t data_column_count = 5UL;

      static const bool versioned = false;

      static const char insert_statement[];
      static const char select_statement[];
      static const char delete_statement[];
    };

    using object_traits<object_type>::id;

    static id_type
    id (const image_type&);

    static bool
    grow (image_type&,
          bool*);

    static void
    bind (sqlite::bind*,
          image_type&,
          sqlite::statement_kind);

    static void
    bind (sqlite::bind*, id_image_type&);

    static bool
    init (image_type&,
          const object_type&,
          sqlite::statement_kind);

    static void
    init (object_type&,
          const image_type&,
          database*);

    static void
    init (id_image_type&, const id_type&);

    typedef sqlite::object_statements<object_type> statements_type;

    typedef sqlite::query_base query_base_type;

    static const std::size_t column_count = 8UL;
    static const std::size_t id_column_count = 2UL;
    static const std::size_t inverse_column_count = 0UL;
    static const std::size_t readonly_column_count = 0UL;
    static const std::size_t managed_optimistic_column_count = 0UL;

    static const std::size_t separate_load_column_count = 0UL;
    static const std::size_t separate_update_column_count = 0UL;

    static const bool versioned = false;

    static const char persist_statement[];
    static const char find_statement[];
    static const char update_statement[];
    static const char erase_statement[];
    static const char query_statement[];
    static const char erase_query_statement[];

    static const char table_name[];

    static void
    persist (database&, const object_type&);

    static pointer_type
    find (database&, const id_type&);

    static bool
    find (database&, const id_type&, object_type&);

    static bool
    reload (database&, object_type&);

    static void
    update (database&, const object_type&);

    static void
    erase (database&, const id_type&);

    static void
    erase (database&, const object_type&);

    static result<object_type>
    query (database&, const query_base_type&);

    static unsigned long long
    erase_query (database&, const query_base_type&);

    public:
    static bool
    find_ (statements_type&,
           const id_type*);

    static void
    load_ (statements_type&,
           object_type&,
           bool reload);
  };

  template <>
  class access::object_traits_impl< ::person, id_common >:
    public access::object_traits_impl< ::person, id_sqlite >
  {
  };

  // person
  //
}

#include "person-odb.ixx"

#include <odb/post.hxx>

#endif // PERSON_ODB_HXX
