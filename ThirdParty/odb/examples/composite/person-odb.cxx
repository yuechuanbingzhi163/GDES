// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "person-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy

#include <odb/schema-catalog-impl.hxx>

#include <odb/sqlite/traits.hxx>
#include <odb/sqlite/database.hxx>
#include <odb/sqlite/transaction.hxx>
#include <odb/sqlite/connection.hxx>
#include <odb/sqlite/statement.hxx>
#include <odb/sqlite/statement-cache.hxx>
#include <odb/sqlite/simple-object-statements.hxx>
#include <odb/sqlite/container-statements.hxx>
#include <odb/sqlite/exceptions.hxx>
#include <odb/sqlite/simple-object-result.hxx>

namespace odb
{
  // basic_name
  //

  bool access::composite_value_traits< ::basic_name, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // first_
    //
    if (t[0UL])
    {
      i.first_value.capacity (i.first_size);
      grew = true;
    }

    // last_
    //
    if (t[1UL])
    {
      i.last_value.capacity (i.last_size);
      grew = true;
    }

    return grew;
  }

  void access::composite_value_traits< ::basic_name, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // first_
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.first_value.data ();
    b[n].size = &i.first_size;
    b[n].capacity = i.first_value.capacity ();
    b[n].is_null = &i.first_null;
    n++;

    // last_
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.last_value.data ();
    b[n].size = &i.last_size;
    b[n].capacity = i.last_value.capacity ();
    b[n].is_null = &i.last_null;
    n++;
  }

  bool access::composite_value_traits< ::basic_name, id_sqlite >::
  init (image_type& i,
        const value_type& o,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    bool grew (false);

    // first_
    //
    {
      ::std::string const& v =
        o.first_;

      bool is_null (false);
      std::size_t cap (i.first_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i.first_value,
        i.first_size,
        is_null,
        v);
      i.first_null = is_null;
      grew = grew || (cap != i.first_value.capacity ());
    }

    // last_
    //
    {
      ::std::string const& v =
        o.last_;

      bool is_null (false);
      std::size_t cap (i.last_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i.last_value,
        i.last_size,
        is_null,
        v);
      i.last_null = is_null;
      grew = grew || (cap != i.last_value.capacity ());
    }

    return grew;
  }

  void access::composite_value_traits< ::basic_name, id_sqlite >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // first_
    //
    {
      ::std::string& v =
        o.first_;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.first_value,
        i.first_size,
        i.first_null);
    }

    // last_
    //
    {
      ::std::string& v =
        o.last_;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.last_value,
        i.last_size,
        i.last_null);
    }
  }

  // name_extras
  //

  // aliases_
  //

  void access::composite_value_traits< ::name_extras, id_sqlite >::aliases_traits::
  bind (sqlite::bind* b,
        const sqlite::bind* id,
        std::size_t id_size,
        data_image_type& d)
  {
    using namespace sqlite;

    statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    size_t n (0);

    // object_id
    //
    if (id != 0)
      std::memcpy (&b[n], id, id_size * sizeof (id[0]));
    n += id_size;

    // index
    //
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &d.index_value;
    b[n].is_null = &d.index_null;
    n++;

    // value
    //
    composite_value_traits< value_type, id_sqlite >::bind (
      b + n, d.value_value, sk);
  }

  void access::composite_value_traits< ::name_extras, id_sqlite >::aliases_traits::
  grow (data_image_type& i,
        bool* t)
  {
    bool grew (false);

    // index
    //
    t[0UL] = false;

    // value
    //
    if (composite_value_traits< value_type, id_sqlite >::grow (
          i.value_value, t + 1UL))
      grew = true;

    if (grew)
      i.version++;
  }

  void access::composite_value_traits< ::name_extras, id_sqlite >::aliases_traits::
  init (data_image_type& i,
        index_type* j,
        const value_type& v)
  {
    using namespace sqlite;

    statement_kind sk (statement_insert);
    ODB_POTENTIALLY_UNUSED (sk);

    bool grew (false);

    // index
    //
    if (j != 0)
    {
      bool is_null (false);
      sqlite::value_traits<
          index_type,
          sqlite::id_integer >::set_image (
        i.index_value,
        is_null,
        *j);
      i.index_null = is_null;
    }

    // value
    //
    {
      if (composite_value_traits< value_type, id_sqlite >::init (
            i.value_value,
            v,
            sk))
        grew = true;
    }

    if (grew)
      i.version++;
  }

  void access::composite_value_traits< ::name_extras, id_sqlite >::aliases_traits::
  init (index_type& j,
        value_type& v,
        const data_image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (db);

    // index
    //
    {
      sqlite::value_traits<
          index_type,
          sqlite::id_integer >::set_value (
        j,
        i.index_value,
        i.index_null);
    }

    // value
    //
    {
      composite_value_traits< value_type, id_sqlite >::init (
        v,
        i.value_value,
        db);
    }
  }

  void access::composite_value_traits< ::name_extras, id_sqlite >::aliases_traits::
  insert (index_type i, const value_type& v, void* d)
  {
    using namespace sqlite;

    statements_type& sts (*static_cast< statements_type* > (d));
    data_image_type& di (sts.data_image ());

    init (di, &i, v);

    if (sts.data_binding_test_version ())
    {
      const binding& id (sts.id_binding ());
      bind (sts.data_bind (), id.bind, id.count, di);
      sts.data_binding_update_version ();
    }

    if (!sts.insert_statement ().execute ())
      throw object_already_persistent ();
  }

  bool access::composite_value_traits< ::name_extras, id_sqlite >::aliases_traits::
  select (index_type& i, value_type& v, void* d)
  {
    using namespace sqlite;
    using sqlite::select_statement;

    statements_type& sts (*static_cast< statements_type* > (d));
    data_image_type& di (sts.data_image ());

    init (i, v, di, &sts.connection ().database ());

    select_statement& st (sts.select_statement ());
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      grow (di, sts.select_image_truncated ());

      if (sts.data_binding_test_version ())
      {
        bind (sts.data_bind (), 0, sts.id_binding ().count, di);
        sts.data_binding_update_version ();
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  void access::composite_value_traits< ::name_extras, id_sqlite >::aliases_traits::
  delete_ (void* d)
  {
    using namespace sqlite;

    statements_type& sts (*static_cast< statements_type* > (d));
    sts.delete_statement ().execute ();
  }

  void access::composite_value_traits< ::name_extras, id_sqlite >::aliases_traits::
  persist (const container_type& c,
           statements_type& sts)
  {
    using namespace sqlite;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::persist (c, fs);
  }

  void access::composite_value_traits< ::name_extras, id_sqlite >::aliases_traits::
  load (container_type& c,
        statements_type& sts)
  {
    using namespace sqlite;
    using sqlite::select_statement;

    const binding& id (sts.id_binding ());

    if (sts.data_binding_test_version ())
    {
      bind (sts.data_bind (), id.bind, id.count, sts.data_image ());
      sts.data_binding_update_version ();
    }

    select_statement& st (sts.select_statement ());
    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      data_image_type& di (sts.data_image ());
      grow (di, sts.select_image_truncated ());

      if (sts.data_binding_test_version ())
      {
        bind (sts.data_bind (), 0, id.count, di);
        sts.data_binding_update_version ();
        st.refetch ();
      }
    }

    bool more (r != select_statement::no_data);

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::load (c, more, fs);
  }

  void access::composite_value_traits< ::name_extras, id_sqlite >::aliases_traits::
  update (const container_type& c,
          statements_type& sts)
  {
    using namespace sqlite;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::update (c, fs);
  }

  void access::composite_value_traits< ::name_extras, id_sqlite >::aliases_traits::
  erase (statements_type& sts)
  {
    using namespace sqlite;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::erase (fs);
  }

  bool access::composite_value_traits< ::name_extras, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // nickname_
    //
    if (t[0UL])
    {
      i.nickname_value.capacity (i.nickname_size);
      grew = true;
    }

    return grew;
  }

  void access::composite_value_traits< ::name_extras, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // nickname_
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.nickname_value.data ();
    b[n].size = &i.nickname_size;
    b[n].capacity = i.nickname_value.capacity ();
    b[n].is_null = &i.nickname_null;
    n++;
  }

  bool access::composite_value_traits< ::name_extras, id_sqlite >::
  init (image_type& i,
        const value_type& o,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    bool grew (false);

    // nickname_
    //
    {
      ::std::string const& v =
        o.nickname_;

      bool is_null (false);
      std::size_t cap (i.nickname_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i.nickname_value,
        i.nickname_size,
        is_null,
        v);
      i.nickname_null = is_null;
      grew = grew || (cap != i.nickname_value.capacity ());
    }

    return grew;
  }

  void access::composite_value_traits< ::name_extras, id_sqlite >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // nickname_
    //
    {
      ::std::string& v =
        o.nickname_;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.nickname_value,
        i.nickname_size,
        i.nickname_null);
    }
  }

  // name
  //

  bool access::composite_value_traits< ::name, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // basic_name base
    //
    if (composite_value_traits< ::basic_name, id_sqlite >::grow (
          i, t + 0UL))
      grew = true;

    // title_
    //
    if (t[2UL])
    {
      i.title_value.capacity (i.title_size);
      grew = true;
    }

    // extras_
    //
    if (composite_value_traits< ::name_extras, id_sqlite >::grow (
          i.extras_value, t + 3UL))
      grew = true;

    return grew;
  }

  void access::composite_value_traits< ::name, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // basic_name base
    //
    composite_value_traits< ::basic_name, id_sqlite >::bind (b + n, i, sk);
    n += 2UL;

    // title_
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.title_value.data ();
    b[n].size = &i.title_size;
    b[n].capacity = i.title_value.capacity ();
    b[n].is_null = &i.title_null;
    n++;

    // extras_
    //
    composite_value_traits< ::name_extras, id_sqlite >::bind (
      b + n, i.extras_value, sk);
    n += 1UL;
  }

  bool access::composite_value_traits< ::name, id_sqlite >::
  init (image_type& i,
        const value_type& o,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    bool grew (false);

    // basic_name base
    //
    if (composite_value_traits< ::basic_name, id_sqlite >::init (i, o, sk))
      grew = true;

    // title_
    //
    {
      ::std::string const& v =
        o.title_;

      bool is_null (false);
      std::size_t cap (i.title_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i.title_value,
        i.title_size,
        is_null,
        v);
      i.title_null = is_null;
      grew = grew || (cap != i.title_value.capacity ());
    }

    // extras_
    //
    {
      ::name_extras const& v =
        o.extras_;

      if (composite_value_traits< ::name_extras, id_sqlite >::init (
            i.extras_value,
            v,
            sk))
        grew = true;
    }

    return grew;
  }

  void access::composite_value_traits< ::name, id_sqlite >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // basic_name base
    //
    composite_value_traits< ::basic_name, id_sqlite >::init (o, i, db);

    // title_
    //
    {
      ::std::string& v =
        o.title_;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.title_value,
        i.title_size,
        i.title_null);
    }

    // extras_
    //
    {
      ::name_extras& v =
        o.extras_;

      composite_value_traits< ::name_extras, id_sqlite >::init (
        v,
        i.extras_value,
        db);
    }
  }

  // phone_numbers
  //

  bool access::composite_value_traits< ::phone_numbers, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // first
    //
    if (t[0UL])
    {
      i.first_value.capacity (i.first_size);
      grew = true;
    }

    // second
    //
    if (t[1UL])
    {
      i.second_value.capacity (i.second_size);
      grew = true;
    }

    return grew;
  }

  void access::composite_value_traits< ::phone_numbers, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // first
    //
    b[n].type = sqlite::image_traits<
      ::std::basic_string< char >,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.first_value.data ();
    b[n].size = &i.first_size;
    b[n].capacity = i.first_value.capacity ();
    b[n].is_null = &i.first_null;
    n++;

    // second
    //
    b[n].type = sqlite::image_traits<
      ::std::basic_string< char >,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.second_value.data ();
    b[n].size = &i.second_size;
    b[n].capacity = i.second_value.capacity ();
    b[n].is_null = &i.second_null;
    n++;
  }

  bool access::composite_value_traits< ::phone_numbers, id_sqlite >::
  init (image_type& i,
        const value_type& o,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    bool grew (false);

    // first
    //
    {
      ::std::basic_string< char > const& v =
        o.first;

      bool is_null (false);
      std::size_t cap (i.first_value.capacity ());
      sqlite::value_traits<
          ::std::basic_string< char >,
          sqlite::id_text >::set_image (
        i.first_value,
        i.first_size,
        is_null,
        v);
      i.first_null = is_null;
      grew = grew || (cap != i.first_value.capacity ());
    }

    // second
    //
    {
      ::std::basic_string< char > const& v =
        o.second;

      bool is_null (false);
      std::size_t cap (i.second_value.capacity ());
      sqlite::value_traits<
          ::std::basic_string< char >,
          sqlite::id_text >::set_image (
        i.second_value,
        i.second_size,
        is_null,
        v);
      i.second_null = is_null;
      grew = grew || (cap != i.second_value.capacity ());
    }

    return grew;
  }

  void access::composite_value_traits< ::phone_numbers, id_sqlite >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // first
    //
    {
      ::std::basic_string< char >& v =
        o.first;

      sqlite::value_traits<
          ::std::basic_string< char >,
          sqlite::id_text >::set_value (
        v,
        i.first_value,
        i.first_size,
        i.first_null);
    }

    // second
    //
    {
      ::std::basic_string< char >& v =
        o.second;

      sqlite::value_traits<
          ::std::basic_string< char >,
          sqlite::id_text >::set_value (
        v,
        i.second_value,
        i.second_size,
        i.second_null);
    }
  }

  // email_address
  //

  bool access::composite_value_traits< ::email_address, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // recipient_
    //
    if (t[0UL])
    {
      i.recipient_value.capacity (i.recipient_size);
      grew = true;
    }

    // domain_
    //
    if (t[1UL])
    {
      i.domain_value.capacity (i.domain_size);
      grew = true;
    }

    return grew;
  }

  void access::composite_value_traits< ::email_address, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // recipient_
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.recipient_value.data ();
    b[n].size = &i.recipient_size;
    b[n].capacity = i.recipient_value.capacity ();
    b[n].is_null = &i.recipient_null;
    n++;

    // domain_
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.domain_value.data ();
    b[n].size = &i.domain_size;
    b[n].capacity = i.domain_value.capacity ();
    b[n].is_null = &i.domain_null;
    n++;
  }

  bool access::composite_value_traits< ::email_address, id_sqlite >::
  init (image_type& i,
        const value_type& o,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    bool grew (false);

    // recipient_
    //
    {
      ::std::string const& v =
        o.recipient_;

      bool is_null (false);
      std::size_t cap (i.recipient_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i.recipient_value,
        i.recipient_size,
        is_null,
        v);
      i.recipient_null = is_null;
      grew = grew || (cap != i.recipient_value.capacity ());
    }

    // domain_
    //
    {
      ::std::string const& v =
        o.domain_;

      bool is_null (false);
      std::size_t cap (i.domain_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i.domain_value,
        i.domain_size,
        is_null,
        v);
      i.domain_null = is_null;
      grew = grew || (cap != i.domain_value.capacity ());
    }

    return grew;
  }

  void access::composite_value_traits< ::email_address, id_sqlite >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // recipient_
    //
    {
      ::std::string& v =
        o.recipient_;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.recipient_value,
        i.recipient_size,
        i.recipient_null);
    }

    // domain_
    //
    {
      ::std::string& v =
        o.domain_;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.domain_value,
        i.domain_size,
        i.domain_null);
    }
  }

  // person
  //

  struct access::object_traits_impl< ::person, id_sqlite >::extra_statement_cache_type
  {
    sqlite::container_statements_impl< name_extras_aliases_traits > name_extras_aliases_;

    extra_statement_cache_type (
      sqlite::connection& c,
      image_type&,
      id_image_type&,
      sqlite::binding& id,
      sqlite::binding&)
    : name_extras_aliases_ (c, id)
    {
    }
  };

  // aliases_
  //

  const char access::object_traits_impl< ::person, id_sqlite >::name_extras_aliases_traits::
  select_statement[] =
  "SELECT "
  "\"c_person_name_extras_aliases\".\"index\", "
  "\"c_person_name_extras_aliases\".\"value_first\", "
  "\"c_person_name_extras_aliases\".\"value_last\" "
  "FROM \"c_person_name_extras_aliases\" "
  "WHERE \"c_person_name_extras_aliases\".\"object_id_recipient\"=? AND \"c_person_name_extras_aliases\".\"object_id_domain\"=? ORDER BY \"c_person_name_extras_aliases\".\"index\"";

  const char access::object_traits_impl< ::person, id_sqlite >::name_extras_aliases_traits::
  insert_statement[] =
  "INSERT INTO \"c_person_name_extras_aliases\" "
  "(\"object_id_recipient\", "
  "\"object_id_domain\", "
  "\"index\", "
  "\"value_first\", "
  "\"value_last\") "
  "VALUES "
  "(?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::person, id_sqlite >::name_extras_aliases_traits::
  delete_statement[] =
  "DELETE FROM \"c_person_name_extras_aliases\" "
  "WHERE \"object_id_recipient\"=? AND \"object_id_domain\"=?";

  access::object_traits_impl< ::person, id_sqlite >::id_type
  access::object_traits_impl< ::person, id_sqlite >::
  id (const image_type& i)
  {
    sqlite::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      composite_value_traits< ::email_address, id_sqlite >::init (
        id,
        i.email_value,
        db);
    }

    return id;
  }

  bool access::object_traits_impl< ::person, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // email_
    //
    if (composite_value_traits< ::email_address, id_sqlite >::grow (
          i.email_value, t + 0UL))
      grew = true;

    // name_
    //
    if (composite_value_traits< ::person::name_type, id_sqlite >::grow (
          i.name_value, t + 2UL))
      grew = true;

    // phone_
    //
    if (composite_value_traits< ::phone_numbers, id_sqlite >::grow (
          i.phone_value, t + 6UL))
      grew = true;

    return grew;
  }

  void access::object_traits_impl< ::person, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    std::size_t n (0);

    // email_
    //
    if (sk != statement_update)
    {
      composite_value_traits< ::email_address, id_sqlite >::bind (
        b + n, i.email_value, sk);
      n += 2UL;
    }

    // name_
    //
    composite_value_traits< ::person::name_type, id_sqlite >::bind (
      b + n, i.name_value, sk);
    n += 4UL;

    // phone_
    //
    composite_value_traits< ::phone_numbers, id_sqlite >::bind (
      b + n, i.phone_value, sk);
    n += 2UL;
  }

  void access::object_traits_impl< ::person, id_sqlite >::
  bind (sqlite::bind* b, id_image_type& i)
  {
    std::size_t n (0);
    sqlite::statement_kind sk (sqlite::statement_select);
    composite_value_traits< ::email_address, id_sqlite >::bind (
      b + n, i.id_value, sk);
  }

  bool access::object_traits_impl< ::person, id_sqlite >::
  init (image_type& i,
        const object_type& o,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    bool grew (false);

    // email_
    //
    if (sk == statement_insert)
    {
      ::email_address const& v =
        o.email_;

      if (composite_value_traits< ::email_address, id_sqlite >::init (
            i.email_value,
            v,
            sk))
        grew = true;
    }

    // name_
    //
    {
      ::person::name_type const& v =
        o.name_;

      if (composite_value_traits< ::person::name_type, id_sqlite >::init (
            i.name_value,
            v,
            sk))
        grew = true;
    }

    // phone_
    //
    {
      ::phone_numbers const& v =
        o.phone_;

      if (composite_value_traits< ::phone_numbers, id_sqlite >::init (
            i.phone_value,
            v,
            sk))
        grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::person, id_sqlite >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // email_
    //
    {
      ::email_address& v =
        o.email_;

      composite_value_traits< ::email_address, id_sqlite >::init (
        v,
        i.email_value,
        db);
    }

    // name_
    //
    {
      ::person::name_type& v =
        o.name_;

      composite_value_traits< ::person::name_type, id_sqlite >::init (
        v,
        i.name_value,
        db);
    }

    // phone_
    //
    {
      ::phone_numbers& v =
        o.phone_;

      composite_value_traits< ::phone_numbers, id_sqlite >::init (
        v,
        i.phone_value,
        db);
    }
  }

  void access::object_traits_impl< ::person, id_sqlite >::
  init (id_image_type& i, const id_type& id)
  {
    bool grew (false);
    sqlite::statement_kind sk (sqlite::statement_select);
    {
      if (composite_value_traits< ::email_address, id_sqlite >::init (
            i.id_value,
            id,
            sk))
        grew = true;
    }

    if (grew)
      i.version++;
  }

  const char access::object_traits_impl< ::person, id_sqlite >::persist_statement[] =
  "INSERT INTO \"c_person\" "
  "(\"email_recipient\", "
  "\"email_domain\", "
  "\"name_first\", "
  "\"name_last\", "
  "\"name_title\", "
  "\"name_extras_nickname\", "
  "\"phone_first\", "
  "\"phone_second\") "
  "VALUES "
  "(?, ?, ?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::person, id_sqlite >::find_statement[] =
  "SELECT "
  "\"c_person\".\"email_recipient\", "
  "\"c_person\".\"email_domain\", "
  "\"c_person\".\"name_first\", "
  "\"c_person\".\"name_last\", "
  "\"c_person\".\"name_title\", "
  "\"c_person\".\"name_extras_nickname\", "
  "\"c_person\".\"phone_first\", "
  "\"c_person\".\"phone_second\" "
  "FROM \"c_person\" "
  "WHERE \"c_person\".\"email_recipient\"=? AND \"c_person\".\"email_domain\"=?";

  const char access::object_traits_impl< ::person, id_sqlite >::update_statement[] =
  "UPDATE \"c_person\" "
  "SET "
  "\"name_first\"=?, "
  "\"name_last\"=?, "
  "\"name_title\"=?, "
  "\"name_extras_nickname\"=?, "
  "\"phone_first\"=?, "
  "\"phone_second\"=? "
  "WHERE \"email_recipient\"=? AND \"email_domain\"=?";

  const char access::object_traits_impl< ::person, id_sqlite >::erase_statement[] =
  "DELETE FROM \"c_person\" "
  "WHERE \"email_recipient\"=? AND \"email_domain\"=?";

  const char access::object_traits_impl< ::person, id_sqlite >::query_statement[] =
  "SELECT "
  "\"c_person\".\"email_recipient\", "
  "\"c_person\".\"email_domain\", "
  "\"c_person\".\"name_first\", "
  "\"c_person\".\"name_last\", "
  "\"c_person\".\"name_title\", "
  "\"c_person\".\"name_extras_nickname\", "
  "\"c_person\".\"phone_first\", "
  "\"c_person\".\"phone_second\" "
  "FROM \"c_person\"";

  const char access::object_traits_impl< ::person, id_sqlite >::erase_query_statement[] =
  "DELETE FROM \"c_person\"";

  const char access::object_traits_impl< ::person, id_sqlite >::table_name[] =
  "\"c_person\"";

  void access::object_traits_impl< ::person, id_sqlite >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    id_image_type& i (sts.id_image ());
    init (i, obj.email_);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    extra_statement_cache_type& esc (sts.extra_statement_cache ());

    // name_.extras_.aliases_
    //
    {
      ::basic_names const& v =
        obj.name_.extras_.aliases_;

      name_extras_aliases_traits::persist (
        v,
        esc.name_extras_aliases_);
    }

    callback (db,
              obj,
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::person, id_sqlite >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace sqlite;
    using sqlite::update_statement;

    callback (db, obj, callback_event::pre_update);

    sqlite::transaction& tr (sqlite::transaction::current ());
    sqlite::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.email_);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    extra_statement_cache_type& esc (sts.extra_statement_cache ());

    // name_.extras_.aliases_
    //
    {
      ::basic_names const& v =
        obj.name_.extras_.aliases_;

      name_extras_aliases_traits::update (
        v,
        esc.name_extras_aliases_);
    }

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::person, id_sqlite >::
  erase (database& db, const id_type& id)
  {
    using namespace sqlite;

    ODB_POTENTIALLY_UNUSED (db);

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    extra_statement_cache_type& esc (sts.extra_statement_cache ());

    // name_.extras_.aliases_
    //
    name_extras_aliases_traits::erase (
      esc.name_extras_aliases_);

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::person, id_sqlite >::pointer_type
  access::object_traits_impl< ::person, id_sqlite >::
  find (database& db, const id_type& id)
  {
    using namespace sqlite;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::person, id_sqlite >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::person, id_sqlite >::
  reload (database& db, object_type& obj)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.email_);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::person, id_sqlite >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace sqlite;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  void access::object_traits_impl< ::person, id_sqlite >::
  load_ (statements_type& sts,
         object_type& obj,
         bool reload)
  {
    ODB_POTENTIALLY_UNUSED (reload);

    extra_statement_cache_type& esc (sts.extra_statement_cache ());

    // name_.extras_.aliases_
    //
    {
      ::basic_names& v =
        obj.name_.extras_.aliases_;

      name_extras_aliases_traits::load (
        v,
        esc.name_extras_aliases_);
    }
  }

  result< access::object_traits_impl< ::person, id_sqlite >::object_type >
  access::object_traits_impl< ::person, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        false,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) sqlite::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::person, id_sqlite >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }
}

namespace odb
{
  static bool
  create_schema (database& db, unsigned short pass, bool drop)
  {
    ODB_POTENTIALLY_UNUSED (db);
    ODB_POTENTIALLY_UNUSED (pass);
    ODB_POTENTIALLY_UNUSED (drop);

    if (drop)
    {
      switch (pass)
      {
        case 1:
        {
          return true;
        }
        case 2:
        {
          db.execute ("DROP TABLE IF EXISTS \"c_person_name_extras_aliases\"");
          db.execute ("DROP TABLE IF EXISTS \"c_person\"");
          return false;
        }
      }
    }
    else
    {
      switch (pass)
      {
        case 1:
        {
          db.execute ("CREATE TABLE \"c_person\" (\n"
                      "  \"email_recipient\" TEXT NOT NULL,\n"
                      "  \"email_domain\" TEXT NOT NULL,\n"
                      "  \"name_first\" TEXT NOT NULL,\n"
                      "  \"name_last\" TEXT NOT NULL,\n"
                      "  \"name_title\" TEXT NOT NULL,\n"
                      "  \"name_extras_nickname\" TEXT NOT NULL,\n"
                      "  \"phone_first\" TEXT NOT NULL,\n"
                      "  \"phone_second\" TEXT NOT NULL,\n"
                      "  PRIMARY KEY (\"email_recipient\",\n"
                      "               \"email_domain\"))");
          db.execute ("CREATE TABLE \"c_person_name_extras_aliases\" (\n"
                      "  \"object_id_recipient\" TEXT NOT NULL,\n"
                      "  \"object_id_domain\" TEXT NOT NULL,\n"
                      "  \"index\" INTEGER NOT NULL,\n"
                      "  \"value_first\" TEXT NOT NULL,\n"
                      "  \"value_last\" TEXT NOT NULL,\n"
                      "  CONSTRAINT \"object_id_fk\"\n"
                      "    FOREIGN KEY (\"object_id_recipient\",\n"
                      "                 \"object_id_domain\")\n"
                      "    REFERENCES \"c_person\" (\"email_recipient\",\n"
                      "                           \"email_domain\")\n"
                      "    ON DELETE CASCADE)");
          db.execute ("CREATE INDEX \"c_person_name_extras_aliases_object_id_i\"\n"
                      "  ON \"c_person_name_extras_aliases\" (\n"
                      "    \"object_id_recipient\",\n"
                      "    \"object_id_domain\")");
          db.execute ("CREATE INDEX \"c_person_name_extras_aliases_index_i\"\n"
                      "  ON \"c_person_name_extras_aliases\" (\"index\")");
          return false;
        }
      }
    }

    return false;
  }

  static const schema_catalog_create_entry
  create_schema_entry_ (
    id_sqlite,
    "",
    &create_schema);
}

#include <odb/post.hxx>
