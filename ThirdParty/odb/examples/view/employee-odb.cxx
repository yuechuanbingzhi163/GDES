// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "employee-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy

#include <odb/schema-catalog-impl.hxx>

#include <odb/sqlite/traits.hxx>
#include <odb/sqlite/database.hxx>
#include <odb/sqlite/transaction.hxx>
#include <odb/sqlite/connection.hxx>
#include <odb/sqlite/statement.hxx>
#include <odb/sqlite/statement-cache.hxx>
#include <odb/sqlite/simple-object-statements.hxx>
#include <odb/sqlite/view-statements.hxx>
#include <odb/sqlite/container-statements.hxx>
#include <odb/sqlite/exceptions.hxx>
#include <odb/sqlite/simple-object-result.hxx>
#include <odb/sqlite/view-result.hxx>

namespace odb
{
  // country
  //

  struct access::object_traits_impl< ::country, id_sqlite >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      sqlite::connection&,
      image_type&,
      id_image_type&,
      sqlite::binding&,
      sqlite::binding&)
    {
    }
  };

  access::object_traits_impl< ::country, id_sqlite >::id_type
  access::object_traits_impl< ::country, id_sqlite >::
  id (const image_type& i)
  {
    sqlite::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        id,
        i.code_value,
        i.code_size,
        i.code_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::country, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // code_
    //
    if (t[0UL])
    {
      i.code_value.capacity (i.code_size);
      grew = true;
    }

    // name_
    //
    if (t[1UL])
    {
      i.name_value.capacity (i.name_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::country, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    std::size_t n (0);

    // code_
    //
    if (sk != statement_update)
    {
      b[n].type = sqlite::image_traits<
        ::std::string,
        sqlite::id_text>::bind_value;
      b[n].buffer = i.code_value.data ();
      b[n].size = &i.code_size;
      b[n].capacity = i.code_value.capacity ();
      b[n].is_null = &i.code_null;
      n++;
    }

    // name_
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.name_value.data ();
    b[n].size = &i.name_size;
    b[n].capacity = i.name_value.capacity ();
    b[n].is_null = &i.name_null;
    n++;
  }

  void access::object_traits_impl< ::country, id_sqlite >::
  bind (sqlite::bind* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.id_value.data ();
    b[n].size = &i.id_size;
    b[n].capacity = i.id_value.capacity ();
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::country, id_sqlite >::
  init (image_type& i,
        const object_type& o,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    bool grew (false);

    // code_
    //
    if (sk == statement_insert)
    {
      ::std::string const& v =
        o.code_;

      bool is_null (false);
      std::size_t cap (i.code_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i.code_value,
        i.code_size,
        is_null,
        v);
      i.code_null = is_null;
      grew = grew || (cap != i.code_value.capacity ());
    }

    // name_
    //
    {
      ::std::string const& v =
        o.name_;

      bool is_null (false);
      std::size_t cap (i.name_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i.name_value,
        i.name_size,
        is_null,
        v);
      i.name_null = is_null;
      grew = grew || (cap != i.name_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::country, id_sqlite >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // code_
    //
    {
      ::std::string& v =
        o.code_;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.code_value,
        i.code_size,
        i.code_null);
    }

    // name_
    //
    {
      ::std::string& v =
        o.name_;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.name_value,
        i.name_size,
        i.name_null);
    }
  }

  void access::object_traits_impl< ::country, id_sqlite >::
  init (id_image_type& i, const id_type& id)
  {
    bool grew (false);
    {
      bool is_null (false);
      std::size_t cap (i.id_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i.id_value,
        i.id_size,
        is_null,
        id);
      i.id_null = is_null;
      grew = grew || (cap != i.id_value.capacity ());
    }

    if (grew)
      i.version++;
  }

  const char access::object_traits_impl< ::country, id_sqlite >::persist_statement[] =
  "INSERT INTO \"view_country\" "
  "(\"code\", "
  "\"name\") "
  "VALUES "
  "(?, ?)";

  const char access::object_traits_impl< ::country, id_sqlite >::find_statement[] =
  "SELECT "
  "\"view_country\".\"code\", "
  "\"view_country\".\"name\" "
  "FROM \"view_country\" "
  "WHERE \"view_country\".\"code\"=?";

  const char access::object_traits_impl< ::country, id_sqlite >::update_statement[] =
  "UPDATE \"view_country\" "
  "SET "
  "\"name\"=? "
  "WHERE \"code\"=?";

  const char access::object_traits_impl< ::country, id_sqlite >::erase_statement[] =
  "DELETE FROM \"view_country\" "
  "WHERE \"code\"=?";

  const char access::object_traits_impl< ::country, id_sqlite >::query_statement[] =
  "SELECT "
  "\"view_country\".\"code\", "
  "\"view_country\".\"name\" "
  "FROM \"view_country\"";

  const char access::object_traits_impl< ::country, id_sqlite >::erase_query_statement[] =
  "DELETE FROM \"view_country\"";

  const char access::object_traits_impl< ::country, id_sqlite >::table_name[] =
  "\"view_country\"";

  void access::object_traits_impl< ::country, id_sqlite >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    callback (db,
              obj,
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::country, id_sqlite >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace sqlite;
    using sqlite::update_statement;

    callback (db, obj, callback_event::pre_update);

    sqlite::transaction& tr (sqlite::transaction::current ());
    sqlite::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.code_);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::country, id_sqlite >::
  erase (database& db, const id_type& id)
  {
    using namespace sqlite;

    ODB_POTENTIALLY_UNUSED (db);

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::country, id_sqlite >::pointer_type
  access::object_traits_impl< ::country, id_sqlite >::
  find (database& db, const id_type& id)
  {
    using namespace sqlite;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::country, id_sqlite >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::country, id_sqlite >::
  reload (database& db, object_type& obj)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.code_);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::country, id_sqlite >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace sqlite;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::country, id_sqlite >::object_type >
  access::object_traits_impl< ::country, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        false,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) sqlite::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::country, id_sqlite >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }

  // employer
  //

  struct access::object_traits_impl< ::employer, id_sqlite >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      sqlite::connection&,
      image_type&,
      id_image_type&,
      sqlite::binding&,
      sqlite::binding&)
    {
    }
  };

  access::object_traits_impl< ::employer, id_sqlite >::id_type
  access::object_traits_impl< ::employer, id_sqlite >::
  id (const image_type& i)
  {
    sqlite::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      sqlite::value_traits<
          long unsigned int,
          sqlite::id_integer >::set_value (
        id,
        i.id_value,
        i.id_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::employer, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // id_
    //
    t[0UL] = false;

    // name_
    //
    if (t[1UL])
    {
      i.name_value.capacity (i.name_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::employer, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    std::size_t n (0);

    // id_
    //
    if (sk != statement_update)
    {
      b[n].type = sqlite::bind::integer;
      b[n].buffer = &i.id_value;
      b[n].is_null = &i.id_null;
      n++;
    }

    // name_
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.name_value.data ();
    b[n].size = &i.name_size;
    b[n].capacity = i.name_value.capacity ();
    b[n].is_null = &i.name_null;
    n++;
  }

  void access::object_traits_impl< ::employer, id_sqlite >::
  bind (sqlite::bind* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::employer, id_sqlite >::
  init (image_type& i,
        const object_type& o,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    bool grew (false);

    // id_
    //
    if (sk == statement_insert)
    {
      long unsigned int const& v =
        o.id_;

      bool is_null (false);
      sqlite::value_traits<
          long unsigned int,
          sqlite::id_integer >::set_image (
        i.id_value,
        is_null,
        v);
      i.id_null = is_null;
    }

    // name_
    //
    {
      ::std::string const& v =
        o.name_;

      bool is_null (false);
      std::size_t cap (i.name_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i.name_value,
        i.name_size,
        is_null,
        v);
      i.name_null = is_null;
      grew = grew || (cap != i.name_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::employer, id_sqlite >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // id_
    //
    {
      long unsigned int& v =
        o.id_;

      sqlite::value_traits<
          long unsigned int,
          sqlite::id_integer >::set_value (
        v,
        i.id_value,
        i.id_null);
    }

    // name_
    //
    {
      ::std::string& v =
        o.name_;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.name_value,
        i.name_size,
        i.name_null);
    }
  }

  void access::object_traits_impl< ::employer, id_sqlite >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      sqlite::value_traits<
          long unsigned int,
          sqlite::id_integer >::set_image (
        i.id_value,
        is_null,
        id);
      i.id_null = is_null;
    }
  }

  const char access::object_traits_impl< ::employer, id_sqlite >::persist_statement[] =
  "INSERT INTO \"view_employer\" "
  "(\"id\", "
  "\"name\") "
  "VALUES "
  "(?, ?)";

  const char access::object_traits_impl< ::employer, id_sqlite >::find_statement[] =
  "SELECT "
  "\"view_employer\".\"id\", "
  "\"view_employer\".\"name\" "
  "FROM \"view_employer\" "
  "WHERE \"view_employer\".\"id\"=?";

  const char access::object_traits_impl< ::employer, id_sqlite >::update_statement[] =
  "UPDATE \"view_employer\" "
  "SET "
  "\"name\"=? "
  "WHERE \"id\"=?";

  const char access::object_traits_impl< ::employer, id_sqlite >::erase_statement[] =
  "DELETE FROM \"view_employer\" "
  "WHERE \"id\"=?";

  const char access::object_traits_impl< ::employer, id_sqlite >::query_statement[] =
  "SELECT "
  "\"view_employer\".\"id\", "
  "\"view_employer\".\"name\" "
  "FROM \"view_employer\"";

  const char access::object_traits_impl< ::employer, id_sqlite >::erase_query_statement[] =
  "DELETE FROM \"view_employer\"";

  const char access::object_traits_impl< ::employer, id_sqlite >::table_name[] =
  "\"view_employer\"";

  void access::object_traits_impl< ::employer, id_sqlite >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    callback (db,
              obj,
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::employer, id_sqlite >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace sqlite;
    using sqlite::update_statement;

    callback (db, obj, callback_event::pre_update);

    sqlite::transaction& tr (sqlite::transaction::current ());
    sqlite::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.id_);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::employer, id_sqlite >::
  erase (database& db, const id_type& id)
  {
    using namespace sqlite;

    ODB_POTENTIALLY_UNUSED (db);

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::employer, id_sqlite >::pointer_type
  access::object_traits_impl< ::employer, id_sqlite >::
  find (database& db, const id_type& id)
  {
    using namespace sqlite;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::employer, id_sqlite >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::employer, id_sqlite >::
  reload (database& db, object_type& obj)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.id_);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::employer, id_sqlite >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace sqlite;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::employer, id_sqlite >::object_type >
  access::object_traits_impl< ::employer, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        false,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) sqlite::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::employer, id_sqlite >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }

  // employee
  //

  const char alias_traits<  ::country,
    id_sqlite,
    access::object_traits_impl< ::employee, id_sqlite >::residence_tag>::
  table_name[] = "\"residence\"";

  const char alias_traits<  ::country,
    id_sqlite,
    access::object_traits_impl< ::employee, id_sqlite >::nationality_tag>::
  table_name[] = "\"nationality\"";

  const char alias_traits<  ::employer,
    id_sqlite,
    access::object_traits_impl< ::employee, id_sqlite >::employed_by_tag>::
  table_name[] = "\"employed_by\"";

  struct access::object_traits_impl< ::employee, id_sqlite >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      sqlite::connection&,
      image_type&,
      id_image_type&,
      sqlite::binding&,
      sqlite::binding&)
    {
    }
  };

  access::object_traits_impl< ::employee, id_sqlite >::id_type
  access::object_traits_impl< ::employee, id_sqlite >::
  id (const image_type& i)
  {
    sqlite::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      sqlite::value_traits<
          long unsigned int,
          sqlite::id_integer >::set_value (
        id,
        i.id_value,
        i.id_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::employee, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // id_
    //
    t[0UL] = false;

    // first_
    //
    if (t[1UL])
    {
      i.first_value.capacity (i.first_size);
      grew = true;
    }

    // last_
    //
    if (t[2UL])
    {
      i.last_value.capacity (i.last_size);
      grew = true;
    }

    // age_
    //
    t[3UL] = false;

    // residence_
    //
    if (t[4UL])
    {
      i.residence_value.capacity (i.residence_size);
      grew = true;
    }

    // nationality_
    //
    if (t[5UL])
    {
      i.nationality_value.capacity (i.nationality_size);
      grew = true;
    }

    // employed_by_
    //
    t[6UL] = false;

    return grew;
  }

  void access::object_traits_impl< ::employee, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    std::size_t n (0);

    // id_
    //
    if (sk != statement_update)
    {
      b[n].type = sqlite::bind::integer;
      b[n].buffer = &i.id_value;
      b[n].is_null = &i.id_null;
      n++;
    }

    // first_
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.first_value.data ();
    b[n].size = &i.first_size;
    b[n].capacity = i.first_value.capacity ();
    b[n].is_null = &i.first_null;
    n++;

    // last_
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.last_value.data ();
    b[n].size = &i.last_size;
    b[n].capacity = i.last_value.capacity ();
    b[n].is_null = &i.last_null;
    n++;

    // age_
    //
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &i.age_value;
    b[n].is_null = &i.age_null;
    n++;

    // residence_
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.residence_value.data ();
    b[n].size = &i.residence_size;
    b[n].capacity = i.residence_value.capacity ();
    b[n].is_null = &i.residence_null;
    n++;

    // nationality_
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.nationality_value.data ();
    b[n].size = &i.nationality_size;
    b[n].capacity = i.nationality_value.capacity ();
    b[n].is_null = &i.nationality_null;
    n++;

    // employed_by_
    //
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &i.employed_by_value;
    b[n].is_null = &i.employed_by_null;
    n++;
  }

  void access::object_traits_impl< ::employee, id_sqlite >::
  bind (sqlite::bind* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::employee, id_sqlite >::
  init (image_type& i,
        const object_type& o,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    bool grew (false);

    // id_
    //
    if (sk == statement_insert)
    {
      long unsigned int const& v =
        o.id_;

      bool is_null (false);
      sqlite::value_traits<
          long unsigned int,
          sqlite::id_integer >::set_image (
        i.id_value,
        is_null,
        v);
      i.id_null = is_null;
    }

    // first_
    //
    {
      ::std::string const& v =
        o.first_;

      bool is_null (false);
      std::size_t cap (i.first_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i.first_value,
        i.first_size,
        is_null,
        v);
      i.first_null = is_null;
      grew = grew || (cap != i.first_value.capacity ());
    }

    // last_
    //
    {
      ::std::string const& v =
        o.last_;

      bool is_null (false);
      std::size_t cap (i.last_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i.last_value,
        i.last_size,
        is_null,
        v);
      i.last_null = is_null;
      grew = grew || (cap != i.last_value.capacity ());
    }

    // age_
    //
    {
      short unsigned int const& v =
        o.age_;

      bool is_null (false);
      sqlite::value_traits<
          short unsigned int,
          sqlite::id_integer >::set_image (
        i.age_value,
        is_null,
        v);
      i.age_null = is_null;
    }

    // residence_
    //
    {
      ::std::tr1::shared_ptr< ::country > const& v =
        o.residence_;

      typedef object_traits< ::country > obj_traits;
      typedef odb::pointer_traits< ::std::tr1::shared_ptr< ::country > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (v));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (v)));

        std::size_t cap (i.residence_value.capacity ());
        sqlite::value_traits<
            obj_traits::id_type,
            sqlite::id_text >::set_image (
          i.residence_value,
          i.residence_size,
          is_null,
          id);
        i.residence_null = is_null;
        grew = grew || (cap != i.residence_value.capacity ());
      }
      else
        i.residence_null = true;
    }

    // nationality_
    //
    {
      ::std::tr1::shared_ptr< ::country > const& v =
        o.nationality_;

      typedef object_traits< ::country > obj_traits;
      typedef odb::pointer_traits< ::std::tr1::shared_ptr< ::country > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (v));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (v)));

        std::size_t cap (i.nationality_value.capacity ());
        sqlite::value_traits<
            obj_traits::id_type,
            sqlite::id_text >::set_image (
          i.nationality_value,
          i.nationality_size,
          is_null,
          id);
        i.nationality_null = is_null;
        grew = grew || (cap != i.nationality_value.capacity ());
      }
      else
        i.nationality_null = true;
    }

    // employed_by_
    //
    {
      ::std::tr1::shared_ptr< ::employer > const& v =
        o.employed_by_;

      typedef object_traits< ::employer > obj_traits;
      typedef odb::pointer_traits< ::std::tr1::shared_ptr< ::employer > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (v));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (v)));

        sqlite::value_traits<
            obj_traits::id_type,
            sqlite::id_integer >::set_image (
          i.employed_by_value,
          is_null,
          id);
        i.employed_by_null = is_null;
      }
      else
        i.employed_by_null = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::employee, id_sqlite >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // id_
    //
    {
      long unsigned int& v =
        o.id_;

      sqlite::value_traits<
          long unsigned int,
          sqlite::id_integer >::set_value (
        v,
        i.id_value,
        i.id_null);
    }

    // first_
    //
    {
      ::std::string& v =
        o.first_;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.first_value,
        i.first_size,
        i.first_null);
    }

    // last_
    //
    {
      ::std::string& v =
        o.last_;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.last_value,
        i.last_size,
        i.last_null);
    }

    // age_
    //
    {
      short unsigned int& v =
        o.age_;

      sqlite::value_traits<
          short unsigned int,
          sqlite::id_integer >::set_value (
        v,
        i.age_value,
        i.age_null);
    }

    // residence_
    //
    {
      ::std::tr1::shared_ptr< ::country >& v =
        o.residence_;

      typedef object_traits< ::country > obj_traits;
      typedef odb::pointer_traits< ::std::tr1::shared_ptr< ::country > > ptr_traits;

      if (i.residence_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        sqlite::value_traits<
            obj_traits::id_type,
            sqlite::id_text >::set_value (
          id,
          i.residence_value,
          i.residence_size,
          i.residence_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<sqlite::database*> (db)->load<
            obj_traits::object_type > (id));
      }
    }

    // nationality_
    //
    {
      ::std::tr1::shared_ptr< ::country >& v =
        o.nationality_;

      typedef object_traits< ::country > obj_traits;
      typedef odb::pointer_traits< ::std::tr1::shared_ptr< ::country > > ptr_traits;

      if (i.nationality_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        sqlite::value_traits<
            obj_traits::id_type,
            sqlite::id_text >::set_value (
          id,
          i.nationality_value,
          i.nationality_size,
          i.nationality_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<sqlite::database*> (db)->load<
            obj_traits::object_type > (id));
      }
    }

    // employed_by_
    //
    {
      ::std::tr1::shared_ptr< ::employer >& v =
        o.employed_by_;

      typedef object_traits< ::employer > obj_traits;
      typedef odb::pointer_traits< ::std::tr1::shared_ptr< ::employer > > ptr_traits;

      if (i.employed_by_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        sqlite::value_traits<
            obj_traits::id_type,
            sqlite::id_integer >::set_value (
          id,
          i.employed_by_value,
          i.employed_by_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<sqlite::database*> (db)->load<
            obj_traits::object_type > (id));
      }
    }
  }

  void access::object_traits_impl< ::employee, id_sqlite >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      sqlite::value_traits<
          long unsigned int,
          sqlite::id_integer >::set_image (
        i.id_value,
        is_null,
        id);
      i.id_null = is_null;
    }
  }

  const char access::object_traits_impl< ::employee, id_sqlite >::persist_statement[] =
  "INSERT INTO \"view_employee\" "
  "(\"id\", "
  "\"first\", "
  "\"last\", "
  "\"age\", "
  "\"residence\", "
  "\"nationality\", "
  "\"employed_by\") "
  "VALUES "
  "(?, ?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::employee, id_sqlite >::find_statement[] =
  "SELECT "
  "\"view_employee\".\"id\", "
  "\"view_employee\".\"first\", "
  "\"view_employee\".\"last\", "
  "\"view_employee\".\"age\", "
  "\"view_employee\".\"residence\", "
  "\"view_employee\".\"nationality\", "
  "\"view_employee\".\"employed_by\" "
  "FROM \"view_employee\" "
  "WHERE \"view_employee\".\"id\"=?";

  const char access::object_traits_impl< ::employee, id_sqlite >::update_statement[] =
  "UPDATE \"view_employee\" "
  "SET "
  "\"first\"=?, "
  "\"last\"=?, "
  "\"age\"=?, "
  "\"residence\"=?, "
  "\"nationality\"=?, "
  "\"employed_by\"=? "
  "WHERE \"id\"=?";

  const char access::object_traits_impl< ::employee, id_sqlite >::erase_statement[] =
  "DELETE FROM \"view_employee\" "
  "WHERE \"id\"=?";

  const char access::object_traits_impl< ::employee, id_sqlite >::query_statement[] =
  "SELECT\n"
  "\"view_employee\".\"id\",\n"
  "\"view_employee\".\"first\",\n"
  "\"view_employee\".\"last\",\n"
  "\"view_employee\".\"age\",\n"
  "\"view_employee\".\"residence\",\n"
  "\"view_employee\".\"nationality\",\n"
  "\"view_employee\".\"employed_by\"\n"
  "FROM \"view_employee\"\n"
  "LEFT JOIN \"view_country\" AS \"residence\" ON \"residence\".\"code\"=\"view_employee\".\"residence\"\n"
  "LEFT JOIN \"view_country\" AS \"nationality\" ON \"nationality\".\"code\"=\"view_employee\".\"nationality\"\n"
  "LEFT JOIN \"view_employer\" AS \"employed_by\" ON \"employed_by\".\"id\"=\"view_employee\".\"employed_by\"";

  const char access::object_traits_impl< ::employee, id_sqlite >::erase_query_statement[] =
  "DELETE FROM \"view_employee\"";

  const char access::object_traits_impl< ::employee, id_sqlite >::table_name[] =
  "\"view_employee\"";

  void access::object_traits_impl< ::employee, id_sqlite >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    callback (db,
              obj,
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::employee, id_sqlite >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace sqlite;
    using sqlite::update_statement;

    callback (db, obj, callback_event::pre_update);

    sqlite::transaction& tr (sqlite::transaction::current ());
    sqlite::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.id_);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::employee, id_sqlite >::
  erase (database& db, const id_type& id)
  {
    using namespace sqlite;

    ODB_POTENTIALLY_UNUSED (db);

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::employee, id_sqlite >::pointer_type
  access::object_traits_impl< ::employee, id_sqlite >::
  find (database& db, const id_type& id)
  {
    using namespace sqlite;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::employee, id_sqlite >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::employee, id_sqlite >::
  reload (database& db, object_type& obj)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.id_);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::employee, id_sqlite >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace sqlite;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::employee, id_sqlite >::object_type >
  access::object_traits_impl< ::employee, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += "\n";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        true,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) sqlite::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::employee, id_sqlite >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }

  // employee_name
  //

  bool access::view_traits_impl< ::employee_name, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // first
    //
    if (t[0UL])
    {
      i.first_value.capacity (i.first_size);
      grew = true;
    }

    // last
    //
    if (t[1UL])
    {
      i.last_value.capacity (i.last_size);
      grew = true;
    }

    return grew;
  }

  void access::view_traits_impl< ::employee_name, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i)
  {
    using namespace sqlite;

    sqlite::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // first
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.first_value.data ();
    b[n].size = &i.first_size;
    b[n].capacity = i.first_value.capacity ();
    b[n].is_null = &i.first_null;
    n++;

    // last
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.last_value.data ();
    b[n].size = &i.last_size;
    b[n].capacity = i.last_value.capacity ();
    b[n].is_null = &i.last_null;
    n++;
  }

  void access::view_traits_impl< ::employee_name, id_sqlite >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // first
    //
    {
      ::std::string& v =
        o.first;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.first_value,
        i.first_size,
        i.first_null);
    }

    // last
    //
    {
      ::std::string& v =
        o.last;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.last_value,
        i.last_size,
        i.last_null);
    }
  }

  access::view_traits_impl< ::employee_name, id_sqlite >::query_base_type
  access::view_traits_impl< ::employee_name, id_sqlite >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT "
      "\"view_employee\".\"first\", "
      "\"view_employee\".\"last\" ");

    r += "FROM \"view_employee\"";

    if (!q.empty ())
    {
      r += " ";
      r += q.clause_prefix ();
      r += q;
    }

    return r;
  }

  result< access::view_traits_impl< ::employee_name, id_sqlite >::view_type >
  access::view_traits_impl< ::employee_name, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) sqlite::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }

  // employee_count
  //

  bool access::view_traits_impl< ::employee_count, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // count
    //
    t[0UL] = false;

    return grew;
  }

  void access::view_traits_impl< ::employee_count, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i)
  {
    using namespace sqlite;

    sqlite::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // count
    //
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &i.count_value;
    b[n].is_null = &i.count_null;
    n++;
  }

  void access::view_traits_impl< ::employee_count, id_sqlite >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // count
    //
    {
      ::std::size_t& v =
        o.count;

      sqlite::value_traits<
          ::std::size_t,
          sqlite::id_integer >::set_value (
        v,
        i.count_value,
        i.count_null);
    }
  }

  access::view_traits_impl< ::employee_count, id_sqlite >::query_base_type
  access::view_traits_impl< ::employee_count, id_sqlite >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT "
      "count(\"view_employee\".\"id\") ");

    r += "FROM \"view_employee\"";

    if (!q.empty ())
    {
      r += " ";
      r += q.clause_prefix ();
      r += q;
    }

    return r;
  }

  result< access::view_traits_impl< ::employee_count, id_sqlite >::view_type >
  access::view_traits_impl< ::employee_count, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) sqlite::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }

  // employee_employer
  //

  bool access::view_traits_impl< ::employee_employer, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // first
    //
    if (t[0UL])
    {
      i.first_value.capacity (i.first_size);
      grew = true;
    }

    // last
    //
    if (t[1UL])
    {
      i.last_value.capacity (i.last_size);
      grew = true;
    }

    // employer_name
    //
    if (t[2UL])
    {
      i.employer_name_value.capacity (i.employer_name_size);
      grew = true;
    }

    return grew;
  }

  void access::view_traits_impl< ::employee_employer, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i)
  {
    using namespace sqlite;

    sqlite::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // first
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.first_value.data ();
    b[n].size = &i.first_size;
    b[n].capacity = i.first_value.capacity ();
    b[n].is_null = &i.first_null;
    n++;

    // last
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.last_value.data ();
    b[n].size = &i.last_size;
    b[n].capacity = i.last_value.capacity ();
    b[n].is_null = &i.last_null;
    n++;

    // employer_name
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.employer_name_value.data ();
    b[n].size = &i.employer_name_size;
    b[n].capacity = i.employer_name_value.capacity ();
    b[n].is_null = &i.employer_name_null;
    n++;
  }

  void access::view_traits_impl< ::employee_employer, id_sqlite >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // first
    //
    {
      ::std::string& v =
        o.first;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.first_value,
        i.first_size,
        i.first_null);
    }

    // last
    //
    {
      ::std::string& v =
        o.last;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.last_value,
        i.last_size,
        i.last_null);
    }

    // employer_name
    //
    {
      ::std::string& v =
        o.employer_name;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.employer_name_value,
        i.employer_name_size,
        i.employer_name_null);
    }
  }

  access::view_traits_impl< ::employee_employer, id_sqlite >::query_base_type
  access::view_traits_impl< ::employee_employer, id_sqlite >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT "
      "\"view_employee\".\"first\", "
      "\"view_employee\".\"last\", "
      "\"view_employer\".\"name\" ");

    r += "FROM \"view_employee\"";

    r += " LEFT JOIN \"view_employer\" ON";
    r += "\"view_employee\".\"employed_by\"=\"view_employer\".\"id\"";

    if (!q.empty ())
    {
      r += " ";
      r += q.clause_prefix ();
      r += q;
    }

    return r;
  }

  result< access::view_traits_impl< ::employee_employer, id_sqlite >::view_type >
  access::view_traits_impl< ::employee_employer, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) sqlite::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }

  // employer_age
  //

  bool access::view_traits_impl< ::employer_age, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // employer_name
    //
    if (t[0UL])
    {
      i.employer_name_value.capacity (i.employer_name_size);
      grew = true;
    }

    // min_age
    //
    t[1UL] = false;

    // max_age
    //
    t[2UL] = false;

    return grew;
  }

  void access::view_traits_impl< ::employer_age, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i)
  {
    using namespace sqlite;

    sqlite::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // employer_name
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.employer_name_value.data ();
    b[n].size = &i.employer_name_size;
    b[n].capacity = i.employer_name_value.capacity ();
    b[n].is_null = &i.employer_name_null;
    n++;

    // min_age
    //
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &i.min_age_value;
    b[n].is_null = &i.min_age_null;
    n++;

    // max_age
    //
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &i.max_age_value;
    b[n].is_null = &i.max_age_null;
    n++;
  }

  void access::view_traits_impl< ::employer_age, id_sqlite >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // employer_name
    //
    {
      ::std::string& v =
        o.employer_name;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.employer_name_value,
        i.employer_name_size,
        i.employer_name_null);
    }

    // min_age
    //
    {
      short unsigned int& v =
        o.min_age;

      sqlite::value_traits<
          short unsigned int,
          sqlite::id_integer >::set_value (
        v,
        i.min_age_value,
        i.min_age_null);
    }

    // max_age
    //
    {
      short unsigned int& v =
        o.max_age;

      sqlite::value_traits<
          short unsigned int,
          sqlite::id_integer >::set_value (
        v,
        i.max_age_value,
        i.max_age_null);
    }
  }

  access::view_traits_impl< ::employer_age, id_sqlite >::query_base_type
  access::view_traits_impl< ::employer_age, id_sqlite >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT "
      "\"view_employer\".\"name\", "
      "min(\"view_employee\".\"age\"), "
      "max(\"view_employee\".\"age\") ");

    r += "FROM \"view_employee\"";

    r += " LEFT JOIN \"view_employer\" ON";
    r += "\"view_employee\".\"employed_by\"=\"view_employer\".\"id\"";

    query_base_type c (
      // From employee.hxx:210:3
      (q.empty () ? query_base_type::true_expr : q) + "GROUP BY" + query_columns::employer::name);

    c.optimize ();

    if (!c.empty ())
    {
      r += " ";
      r += c.clause_prefix ();
      r += c;
    }

    return r;
  }

  result< access::view_traits_impl< ::employer_age, id_sqlite >::view_type >
  access::view_traits_impl< ::employer_age, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) sqlite::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }

  // employee_country
  //

  const char alias_traits<  ::country,
    id_sqlite,
    access::view_traits_impl< ::employee_country, id_sqlite >::res_country_tag>::
  table_name[] = "\"res_country\"";

  const char alias_traits<  ::country,
    id_sqlite,
    access::view_traits_impl< ::employee_country, id_sqlite >::nat_country_tag>::
  table_name[] = "\"nat_country\"";

  bool access::view_traits_impl< ::employee_country, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // first
    //
    if (t[0UL])
    {
      i.first_value.capacity (i.first_size);
      grew = true;
    }

    // last
    //
    if (t[1UL])
    {
      i.last_value.capacity (i.last_size);
      grew = true;
    }

    // res_country_name
    //
    if (t[2UL])
    {
      i.res_country_name_value.capacity (i.res_country_name_size);
      grew = true;
    }

    // nat_country_name
    //
    if (t[3UL])
    {
      i.nat_country_name_value.capacity (i.nat_country_name_size);
      grew = true;
    }

    return grew;
  }

  void access::view_traits_impl< ::employee_country, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i)
  {
    using namespace sqlite;

    sqlite::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // first
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.first_value.data ();
    b[n].size = &i.first_size;
    b[n].capacity = i.first_value.capacity ();
    b[n].is_null = &i.first_null;
    n++;

    // last
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.last_value.data ();
    b[n].size = &i.last_size;
    b[n].capacity = i.last_value.capacity ();
    b[n].is_null = &i.last_null;
    n++;

    // res_country_name
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.res_country_name_value.data ();
    b[n].size = &i.res_country_name_size;
    b[n].capacity = i.res_country_name_value.capacity ();
    b[n].is_null = &i.res_country_name_null;
    n++;

    // nat_country_name
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.nat_country_name_value.data ();
    b[n].size = &i.nat_country_name_size;
    b[n].capacity = i.nat_country_name_value.capacity ();
    b[n].is_null = &i.nat_country_name_null;
    n++;
  }

  void access::view_traits_impl< ::employee_country, id_sqlite >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // first
    //
    {
      ::std::string& v =
        o.first;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.first_value,
        i.first_size,
        i.first_null);
    }

    // last
    //
    {
      ::std::string& v =
        o.last;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.last_value,
        i.last_size,
        i.last_null);
    }

    // res_country_name
    //
    {
      ::std::string& v =
        o.res_country_name;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.res_country_name_value,
        i.res_country_name_size,
        i.res_country_name_null);
    }

    // nat_country_name
    //
    {
      ::std::string& v =
        o.nat_country_name;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.nat_country_name_value,
        i.nat_country_name_size,
        i.nat_country_name_null);
    }
  }

  access::view_traits_impl< ::employee_country, id_sqlite >::query_base_type
  access::view_traits_impl< ::employee_country, id_sqlite >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT "
      "\"view_employee\".\"first\", "
      "\"view_employee\".\"last\", "
      "\"res_country\".\"name\", "
      "\"nat_country\".\"name\" ");

    r += "FROM \"view_employee\"";

    r += " LEFT JOIN \"view_country\" AS \"res_country\" ON";
    r += "\"view_employee\".\"residence\"=\"res_country\".\"code\"";

    r += " LEFT JOIN \"view_country\" AS \"nat_country\" ON";
    r += "\"view_employee\".\"nationality\"=\"nat_country\".\"code\"";

    if (!q.empty ())
    {
      r += " ";
      r += q.clause_prefix ();
      r += q;
    }

    return r;
  }

  result< access::view_traits_impl< ::employee_country, id_sqlite >::view_type >
  access::view_traits_impl< ::employee_country, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) sqlite::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }

  // employee_country_objects
  //

  const char alias_traits<  ::country,
    id_sqlite,
    access::view_traits_impl< ::employee_country_objects, id_sqlite >::res_tag>::
  table_name[] = "\"res\"";

  const char alias_traits<  ::country,
    id_sqlite,
    access::view_traits_impl< ::employee_country_objects, id_sqlite >::nat_tag>::
  table_name[] = "\"nat\"";

  bool access::view_traits_impl< ::employee_country_objects, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // e
    //
    if (object_traits_impl< ::employee, id_sqlite >::grow (
          i.e_value, t + 0UL))
      grew = true;

    // res
    //
    if (object_traits_impl< ::country, id_sqlite >::grow (
          i.res_value, t + 7UL))
      grew = true;

    // nat
    //
    if (object_traits_impl< ::country, id_sqlite >::grow (
          i.nat_value, t + 9UL))
      grew = true;

    return grew;
  }

  void access::view_traits_impl< ::employee_country_objects, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i)
  {
    using namespace sqlite;

    sqlite::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // e
    //
    object_traits_impl< ::employee, id_sqlite >::bind (
      b + n, i.e_value, sk);
    n += 7UL;

    // res
    //
    object_traits_impl< ::country, id_sqlite >::bind (
      b + n, i.res_value, sk);
    n += 2UL;

    // nat
    //
    object_traits_impl< ::country, id_sqlite >::bind (
      b + n, i.nat_value, sk);
    n += 2UL;
  }

  void access::view_traits_impl< ::employee_country_objects, id_sqlite >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    if (!session::has_current ())
      throw session_required ();

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());

    // e pre
    //
    typedef ::employee e_object_type;
    typedef object_traits_impl<e_object_type, id_sqlite> e_object_traits;
    typedef e_object_traits::pointer_type e_pointer_type;
    typedef e_object_traits::pointer_traits e_pointer_traits;
    typedef e_object_traits::pointer_cache_traits e_cache_traits;

    e_object_traits::id_type e_id;
    e_pointer_type e_p;
    e_pointer_traits::guard e_pg;
    e_cache_traits::insert_guard e_ig;
    e_object_type* e_o (0);

    {
      if (!(i.e_value.id_null))
      {
        e_id = e_object_traits::id (i.e_value);
        e_p = e_cache_traits::find (*db, e_id);

        if (e_pointer_traits::null_ptr (e_p))
        {
          e_p = object_factory<e_object_type, e_pointer_type>::create ();
          e_pg.reset (e_p);
          e_ig.reset (e_cache_traits::insert (*db, e_id, e_p));
          e_o = e_pointer_traits::get_ptr (e_p);
        }
      }
    }

    // res pre
    //
    typedef ::country res_object_type;
    typedef object_traits_impl<res_object_type, id_sqlite> res_object_traits;
    typedef res_object_traits::pointer_type res_pointer_type;
    typedef res_object_traits::pointer_traits res_pointer_traits;
    typedef res_object_traits::pointer_cache_traits res_cache_traits;

    res_object_traits::id_type res_id;
    res_pointer_type res_p;
    res_pointer_traits::guard res_pg;
    res_cache_traits::insert_guard res_ig;
    res_object_type* res_o (0);

    {
      if (!(i.res_value.code_null))
      {
        res_id = res_object_traits::id (i.res_value);
        res_p = res_cache_traits::find (*db, res_id);

        if (res_pointer_traits::null_ptr (res_p))
        {
          res_p = object_factory<res_object_type, res_pointer_type>::create ();
          res_pg.reset (res_p);
          res_ig.reset (res_cache_traits::insert (*db, res_id, res_p));
          res_o = res_pointer_traits::get_ptr (res_p);
        }
      }
    }

    // nat pre
    //
    typedef ::country nat_object_type;
    typedef object_traits_impl<nat_object_type, id_sqlite> nat_object_traits;
    typedef nat_object_traits::pointer_type nat_pointer_type;
    typedef nat_object_traits::pointer_traits nat_pointer_traits;
    typedef nat_object_traits::pointer_cache_traits nat_cache_traits;

    nat_object_traits::id_type nat_id;
    nat_pointer_type nat_p;
    nat_pointer_traits::guard nat_pg;
    nat_cache_traits::insert_guard nat_ig;
    nat_object_type* nat_o (0);

    {
      if (!(i.nat_value.code_null))
      {
        nat_id = nat_object_traits::id (i.nat_value);
        nat_p = nat_cache_traits::find (*db, nat_id);

        if (nat_pointer_traits::null_ptr (nat_p))
        {
          nat_p = object_factory<nat_object_type, nat_pointer_type>::create ();
          nat_pg.reset (nat_p);
          nat_ig.reset (nat_cache_traits::insert (*db, nat_id, nat_p));
          nat_o = nat_pointer_traits::get_ptr (nat_p);
        }
      }
    }

    // e
    //
    {
      if (e_o != 0)
      {
        e_object_traits::callback (*db, *e_o, callback_event::pre_load);
        e_object_traits::init (*e_o, i.e_value, db);
        e_object_traits::statements_type& sts (
          conn.statement_cache ().find_object<e_object_type> ());
        e_object_traits::load_ (sts, *e_o, false);
      }
    }

    // res
    //
    {
      if (res_o != 0)
      {
        res_object_traits::callback (*db, *res_o, callback_event::pre_load);
        res_object_traits::init (*res_o, i.res_value, db);
        res_object_traits::statements_type& sts (
          conn.statement_cache ().find_object<res_object_type> ());
        res_object_traits::load_ (sts, *res_o, false);
      }
    }

    // nat
    //
    {
      if (nat_o != 0)
      {
        nat_object_traits::callback (*db, *nat_o, callback_event::pre_load);
        nat_object_traits::init (*nat_o, i.nat_value, db);
        nat_object_traits::statements_type& sts (
          conn.statement_cache ().find_object<nat_object_type> ());
        nat_object_traits::load_ (sts, *nat_o, false);
      }
    }

    // e post
    //
    {
      if (e_o != 0)
      {
        e_object_traits::callback (*db, *e_o, callback_event::post_load);
        e_cache_traits::load (e_ig.position ());
        e_ig.release ();
        e_pg.release ();
      }

      // If a compiler error points to the line below, then
      // it most likely means that a pointer used in view
      // member cannot be initialized from an object pointer.
      //
      o.e = ::std::tr1::shared_ptr< ::employee > (
        e_p);
    }

    // res post
    //
    {
      if (res_o != 0)
      {
        res_object_traits::callback (*db, *res_o, callback_event::post_load);
        res_cache_traits::load (res_ig.position ());
        res_ig.release ();
        res_pg.release ();
      }

      // If a compiler error points to the line below, then
      // it most likely means that a pointer used in view
      // member cannot be initialized from an object pointer.
      //
      o.res = ::std::tr1::shared_ptr< ::country > (
        res_p);
    }

    // nat post
    //
    {
      if (nat_o != 0)
      {
        nat_object_traits::callback (*db, *nat_o, callback_event::post_load);
        nat_cache_traits::load (nat_ig.position ());
        nat_ig.release ();
        nat_pg.release ();
      }

      // If a compiler error points to the line below, then
      // it most likely means that a pointer used in view
      // member cannot be initialized from an object pointer.
      //
      o.nat = ::std::tr1::shared_ptr< ::country > (
        nat_p);
    }
  }

  access::view_traits_impl< ::employee_country_objects, id_sqlite >::query_base_type
  access::view_traits_impl< ::employee_country_objects, id_sqlite >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT "
      "\"view_employee\".\"id\", "
      "\"view_employee\".\"first\", "
      "\"view_employee\".\"last\", "
      "\"view_employee\".\"age\", "
      "\"view_employee\".\"residence\", "
      "\"view_employee\".\"nationality\", "
      "\"view_employee\".\"employed_by\", "
      "\"res\".\"code\", "
      "\"res\".\"name\", "
      "\"nat\".\"code\", "
      "\"nat\".\"name\" ");

    r += "FROM \"view_employee\"";

    r += " LEFT JOIN \"view_country\" AS \"res\" ON";
    r += "\"view_employee\".\"residence\"=\"res\".\"code\"";

    r += " LEFT JOIN \"view_country\" AS \"nat\" ON";
    r += "\"view_employee\".\"nationality\"=\"nat\".\"code\"";

    if (!q.empty ())
    {
      r += " ";
      r += q.clause_prefix ();
      r += q;
    }

    return r;
  }

  result< access::view_traits_impl< ::employee_country_objects, id_sqlite >::view_type >
  access::view_traits_impl< ::employee_country_objects, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) sqlite::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }

  // employer_with_employees
  //

  bool access::view_traits_impl< ::employer_with_employees, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // e
    //
    if (object_traits_impl< ::employer, id_sqlite >::grow (
          i.e_value, t + 0UL))
      grew = true;

    return grew;
  }

  void access::view_traits_impl< ::employer_with_employees, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i)
  {
    using namespace sqlite;

    sqlite::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // e
    //
    object_traits_impl< ::employer, id_sqlite >::bind (
      b + n, i.e_value, sk);
    n += 2UL;
  }

  void access::view_traits_impl< ::employer_with_employees, id_sqlite >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());

    // e pre
    //
    typedef ::employer e_object_type;
    typedef object_traits_impl<e_object_type, id_sqlite> e_object_traits;
    typedef e_object_traits::pointer_type e_pointer_type;
    typedef e_object_traits::pointer_traits e_pointer_traits;
    typedef e_object_traits::pointer_cache_traits e_cache_traits;

    e_object_traits::id_type e_id;
    e_pointer_type e_p;
    e_pointer_traits::guard e_pg;
    e_cache_traits::insert_guard e_ig;
    e_object_type* e_o (0);

    {
      if (!(i.e_value.id_null))
      {
        e_id = e_object_traits::id (i.e_value);
        e_p = e_cache_traits::find (*db, e_id);

        if (e_pointer_traits::null_ptr (e_p))
        {
          e_p = object_factory<e_object_type, e_pointer_type>::create ();
          e_pg.reset (e_p);
          e_ig.reset (e_cache_traits::insert (*db, e_id, e_p));
          e_o = e_pointer_traits::get_ptr (e_p);
        }
      }
    }

    // e
    //
    {
      if (e_o != 0)
      {
        e_object_traits::callback (*db, *e_o, callback_event::pre_load);
        e_object_traits::init (*e_o, i.e_value, db);
        e_object_traits::statements_type& sts (
          conn.statement_cache ().find_object<e_object_type> ());
        e_object_traits::load_ (sts, *e_o, false);
      }
    }

    // e post
    //
    {
      if (e_o != 0)
      {
        e_object_traits::callback (*db, *e_o, callback_event::post_load);
        e_cache_traits::load (e_ig.position ());
        e_ig.release ();
        e_pg.release ();
      }

      // If a compiler error points to the line below, then
      // it most likely means that a pointer used in view
      // member cannot be initialized from an object pointer.
      //
      o.e = ::std::tr1::shared_ptr< ::employer > (
        e_p);
    }
  }

  access::view_traits_impl< ::employer_with_employees, id_sqlite >::query_base_type
  access::view_traits_impl< ::employer_with_employees, id_sqlite >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT DISTINCT "
      "\"view_employer\".\"id\", "
      "\"view_employer\".\"name\" ");

    r += "FROM \"view_employer\"";

    r += " INNER JOIN \"view_employee\" ON";
    r += "\"view_employee\".\"employed_by\"=\"view_employer\".\"id\"";

    if (!q.empty ())
    {
      r += " ";
      r += q.clause_prefix ();
      r += q;
    }

    return r;
  }

  result< access::view_traits_impl< ::employer_with_employees, id_sqlite >::view_type >
  access::view_traits_impl< ::employer_with_employees, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) sqlite::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }

  // employee_vacation
  //

  bool access::view_traits_impl< ::employee_vacation, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // id
    //
    t[0UL] = false;

    // days
    //
    t[1UL] = false;

    return grew;
  }

  void access::view_traits_impl< ::employee_vacation, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i)
  {
    using namespace sqlite;

    sqlite::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // id
    //
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
    n++;

    // days
    //
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &i.days_value;
    b[n].is_null = &i.days_null;
    n++;
  }

  void access::view_traits_impl< ::employee_vacation, id_sqlite >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // id
    //
    {
      long unsigned int& v =
        o.id;

      sqlite::value_traits<
          long unsigned int,
          sqlite::id_integer >::set_value (
        v,
        i.id_value,
        i.id_null);
    }

    // days
    //
    {
      short unsigned int& v =
        o.days;

      sqlite::value_traits<
          short unsigned int,
          sqlite::id_integer >::set_value (
        v,
        i.days_value,
        i.days_null);
    }
  }

  access::view_traits_impl< ::employee_vacation, id_sqlite >::query_base_type
  access::view_traits_impl< ::employee_vacation, id_sqlite >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT employee_id, vacation_days FROM view_employee_extra");

    if (!q.empty ())
    {
      r += " ";
      r += q.clause_prefix ();
      r += q;
    }

    return r;
  }

  result< access::view_traits_impl< ::employee_vacation, id_sqlite >::view_type >
  access::view_traits_impl< ::employee_vacation, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) sqlite::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }

  // employee_vacation1
  //

  bool access::view_traits_impl< ::employee_vacation1, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // id
    //
    t[0UL] = false;

    // days
    //
    t[1UL] = false;

    return grew;
  }

  void access::view_traits_impl< ::employee_vacation1, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i)
  {
    using namespace sqlite;

    sqlite::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // id
    //
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
    n++;

    // days
    //
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &i.days_value;
    b[n].is_null = &i.days_null;
    n++;
  }

  void access::view_traits_impl< ::employee_vacation1, id_sqlite >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // id
    //
    {
      long unsigned int& v =
        o.id;

      sqlite::value_traits<
          long unsigned int,
          sqlite::id_integer >::set_value (
        v,
        i.id_value,
        i.id_null);
    }

    // days
    //
    {
      short unsigned int& v =
        o.days;

      sqlite::value_traits<
          short unsigned int,
          sqlite::id_integer >::set_value (
        v,
        i.days_value,
        i.days_null);
    }
  }

  access::view_traits_impl< ::employee_vacation1, id_sqlite >::query_base_type
  access::view_traits_impl< ::employee_vacation1, id_sqlite >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT "
      "\"employee_id\", "
      "\"vacation_days\" ");

    r += "FROM \"view_employee_extra\"";

    if (!q.empty ())
    {
      r += " ";
      r += q.clause_prefix ();
      r += q;
    }

    return r;
  }

  result< access::view_traits_impl< ::employee_vacation1, id_sqlite >::view_type >
  access::view_traits_impl< ::employee_vacation1, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) sqlite::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }

  // employee_vacation2
  //

  bool access::view_traits_impl< ::employee_vacation2, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // first
    //
    if (t[0UL])
    {
      i.first_value.capacity (i.first_size);
      grew = true;
    }

    // last
    //
    if (t[1UL])
    {
      i.last_value.capacity (i.last_size);
      grew = true;
    }

    // days
    //
    t[2UL] = false;

    return grew;
  }

  void access::view_traits_impl< ::employee_vacation2, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i)
  {
    using namespace sqlite;

    sqlite::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // first
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.first_value.data ();
    b[n].size = &i.first_size;
    b[n].capacity = i.first_value.capacity ();
    b[n].is_null = &i.first_null;
    n++;

    // last
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.last_value.data ();
    b[n].size = &i.last_size;
    b[n].capacity = i.last_value.capacity ();
    b[n].is_null = &i.last_null;
    n++;

    // days
    //
    b[n].type = sqlite::bind::integer;
    b[n].buffer = &i.days_value;
    b[n].is_null = &i.days_null;
    n++;
  }

  void access::view_traits_impl< ::employee_vacation2, id_sqlite >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // first
    //
    {
      ::std::string& v =
        o.first;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.first_value,
        i.first_size,
        i.first_null);
    }

    // last
    //
    {
      ::std::string& v =
        o.last;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.last_value,
        i.last_size,
        i.last_null);
    }

    // days
    //
    {
      short unsigned int& v =
        o.days;

      sqlite::value_traits<
          short unsigned int,
          sqlite::id_integer >::set_value (
        v,
        i.days_value,
        i.days_null);
    }
  }

  access::view_traits_impl< ::employee_vacation2, id_sqlite >::query_base_type
  access::view_traits_impl< ::employee_vacation2, id_sqlite >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT "
      "\"view_employee\".\"first\", "
      "\"view_employee\".\"last\", "
      "\"view_employee_extra\".\"vacation_days\" ");

    r += "FROM \"view_employee_extra\"";

    r += " LEFT JOIN \"view_employee\" ON";
    // From employee.hxx:315:5
    r += "view_employee_extra.employee_id = " + query_columns::id;

    if (!q.empty ())
    {
      r += " ";
      r += q.clause_prefix ();
      r += q;
    }

    return r;
  }

  result< access::view_traits_impl< ::employee_vacation2, id_sqlite >::view_type >
  access::view_traits_impl< ::employee_vacation2, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) sqlite::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }

  // employee_prev_employer
  //

  bool access::view_traits_impl< ::employee_prev_employer, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // first
    //
    if (t[0UL])
    {
      i.first_value.capacity (i.first_size);
      grew = true;
    }

    // last
    //
    if (t[1UL])
    {
      i.last_value.capacity (i.last_size);
      grew = true;
    }

    // prev_employer_name
    //
    if (t[2UL])
    {
      i.prev_employer_name_value.capacity (i.prev_employer_name_size);
      grew = true;
    }

    return grew;
  }

  void access::view_traits_impl< ::employee_prev_employer, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i)
  {
    using namespace sqlite;

    sqlite::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // first
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.first_value.data ();
    b[n].size = &i.first_size;
    b[n].capacity = i.first_value.capacity ();
    b[n].is_null = &i.first_null;
    n++;

    // last
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.last_value.data ();
    b[n].size = &i.last_size;
    b[n].capacity = i.last_value.capacity ();
    b[n].is_null = &i.last_null;
    n++;

    // prev_employer_name
    //
    b[n].type = sqlite::image_traits<
      ::odb::nullable< ::std::basic_string< char > >,
      sqlite::id_text>::bind_value;
    b[n].buffer = i.prev_employer_name_value.data ();
    b[n].size = &i.prev_employer_name_size;
    b[n].capacity = i.prev_employer_name_value.capacity ();
    b[n].is_null = &i.prev_employer_name_null;
    n++;
  }

  void access::view_traits_impl< ::employee_prev_employer, id_sqlite >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // first
    //
    {
      ::std::string& v =
        o.first;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.first_value,
        i.first_size,
        i.first_null);
    }

    // last
    //
    {
      ::std::string& v =
        o.last;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i.last_value,
        i.last_size,
        i.last_null);
    }

    // prev_employer_name
    //
    {
      ::odb::nullable< ::std::basic_string< char > >& v =
        o.prev_employer_name;

      sqlite::value_traits<
          ::odb::nullable< ::std::basic_string< char > >,
          sqlite::id_text >::set_value (
        v,
        i.prev_employer_name_value,
        i.prev_employer_name_size,
        i.prev_employer_name_null);
    }
  }

  access::view_traits_impl< ::employee_prev_employer, id_sqlite >::query_base_type
  access::view_traits_impl< ::employee_prev_employer, id_sqlite >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT "
      "\"view_employee\".\"first\", "
      "\"view_employee\".\"last\", "
      "\"view_employer\".\"name\" ");

    r += "FROM \"view_employee\"";

    r += " LEFT JOIN \"view_employee_extra\" AS \"extra\" ON";
    // From employee.hxx:335:5
    r += "extra.employee_id = " + query_columns::employee::id;

    r += " LEFT JOIN \"view_employer\" ON";
    // From employee.hxx:337:5
    r += "extra.previous_employer_id = " + query_columns::employer::id;

    if (!q.empty ())
    {
      r += " ";
      r += q.clause_prefix ();
      r += q;
    }

    return r;
  }

  result< access::view_traits_impl< ::employee_prev_employer, id_sqlite >::view_type >
  access::view_traits_impl< ::employee_prev_employer, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) sqlite::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }
}

namespace odb
{
  static bool
  create_schema (database& db, unsigned short pass, bool drop)
  {
    ODB_POTENTIALLY_UNUSED (db);
    ODB_POTENTIALLY_UNUSED (pass);
    ODB_POTENTIALLY_UNUSED (drop);

    if (drop)
    {
      switch (pass)
      {
        case 1:
        {
          return true;
        }
        case 2:
        {
          db.execute ("DROP TABLE IF EXISTS \"view_employee\"");
          db.execute ("DROP TABLE IF EXISTS \"view_employer\"");
          db.execute ("DROP TABLE IF EXISTS \"view_country\"");
          return false;
        }
      }
    }
    else
    {
      switch (pass)
      {
        case 1:
        {
          db.execute ("CREATE TABLE \"view_country\" (\n"
                      "  \"code\" TEXT NOT NULL PRIMARY KEY,\n"
                      "  \"name\" TEXT NOT NULL)");
          db.execute ("CREATE TABLE \"view_employer\" (\n"
                      "  \"id\" INTEGER NOT NULL PRIMARY KEY,\n"
                      "  \"name\" TEXT NOT NULL)");
          db.execute ("CREATE TABLE \"view_employee\" (\n"
                      "  \"id\" INTEGER NOT NULL PRIMARY KEY,\n"
                      "  \"first\" TEXT NOT NULL,\n"
                      "  \"last\" TEXT NOT NULL,\n"
                      "  \"age\" INTEGER NOT NULL,\n"
                      "  \"residence\" TEXT NULL,\n"
                      "  \"nationality\" TEXT NULL,\n"
                      "  \"employed_by\" INTEGER NULL,\n"
                      "  CONSTRAINT \"residence_fk\"\n"
                      "    FOREIGN KEY (\"residence\")\n"
                      "    REFERENCES \"view_country\" (\"code\")\n"
                      "    DEFERRABLE INITIALLY DEFERRED,\n"
                      "  CONSTRAINT \"nationality_fk\"\n"
                      "    FOREIGN KEY (\"nationality\")\n"
                      "    REFERENCES \"view_country\" (\"code\")\n"
                      "    DEFERRABLE INITIALLY DEFERRED,\n"
                      "  CONSTRAINT \"employed_by_fk\"\n"
                      "    FOREIGN KEY (\"employed_by\")\n"
                      "    REFERENCES \"view_employer\" (\"id\")\n"
                      "    DEFERRABLE INITIALLY DEFERRED)");
          return false;
        }
      }
    }

    return false;
  }

  static const schema_catalog_create_entry
  create_schema_entry_ (
    id_sqlite,
    "",
    &create_schema);
}

#include <odb/post.hxx>
